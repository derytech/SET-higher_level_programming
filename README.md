# JavaScript Warm Up

This project introduces the basics of JavaScript programming.

## Task 0: First constant, first print

The `0-javascript_is_amazing.js` script creates a constant variable called `myVar` with the value `JavaScript is amazing` and prints it using `console.log()`.

## Usage

Run the script with:

```bash
./0-javascript_is_amazing.js
